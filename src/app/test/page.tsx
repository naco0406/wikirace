
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: '링클 | 게임 클리어',
};

export default function Test() {
    return (
        <div>
            TEST
        </div>
    );
}